name: Atualizar dados diários

on:
  schedule:
    # 09:15 UTC = 06:15 em Brasília (horário padrão)
    - cron: "15 9 * * *"
  workflow_dispatch: {}
  push:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: update-data
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Rodar coleta de novidades
        run: python3 scripts/update_data.py

      - name: Commitar dados atualizados
        run: |
          git config user.name "agente-tributario-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/reforma_tributaria.json data/icms_sre.json data/icms_base_legal.json
          if git diff --cached --quiet; then
            echo "Sem novidades hoje."
          else
            git commit -m "chore: atualizar novidades (reforma tributária e ICMS/SRE)"
            for i in 1 2 3 4 5; do
              if git push; then
                break
              fi
              echo "Push rejeitado (provável corrida com outro workflow), tentando novamente após rebase..."
              sleep $((i * 5))
              git fetch origin main
              git rebase origin/main
            done
          fi
